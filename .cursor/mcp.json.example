{
  "mcpServers": {
    "zendesk": {
      "command": "python3",
      "args": [
        "scripts/zendesk_mcp_server.py",
        "--subdomain", "YOUR_ZENDESK_SUBDOMAIN",
        "--email", "your.email@company.com",
        "--token", "YOUR_ZENDESK_API_TOKEN"
      ]
    },
    "atlassian": {
      "command": "uvx",
      "args": [
        "mcp-atlassian",
        "--jira-url", "https://YOUR_DOMAIN.atlassian.net",
        "--jira-username", "your.email@company.com",
        "--jira-token", "YOUR_ATLASSIAN_API_TOKEN",
        "--confluence-url", "https://YOUR_DOMAIN.atlassian.net/wiki",
        "--confluence-username", "your.email@company.com",
        "--confluence-token", "YOUR_ATLASSIAN_API_TOKEN",
        "--read-only"
      ]
    },
    "github": {
      "command": "uvx",
      "args": ["mcp-github"],
      "env": {
        "GITHUB_TOKEN": "YOUR_GITHUB_PAT"
      }
    },
    "glean_default": {
      "type": "http",
      "url": "https://YOUR_COMPANY.glean.com/mcp/default"
    }
  }
}

